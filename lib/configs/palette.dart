import 'package:flutter/material.dart';

class Palette {
  // Common Colors
  static const primaryColor = Color(0xFF31b768);
  static const darkBlackColor = Color(0xFF000000);
  static const lightWhiteColor = Color(0xFFFFFFFF);
}
